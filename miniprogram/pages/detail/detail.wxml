<view class="inpt">
  <view class="title">诊断</view>
  <view class="title-text">{{title}}</view>
  <view class="cure {{cure ? 'green' : 'blue'}}">{{cure ? '治愈' : '治愈中'}}</view>
</view>
<view class="img">
  <view class="title">诊断证明</view>
  <view class="img-wrap">
    <view class="item" wx:for="{{images}}" wx:key="index"><image src="{{item}}" mode="aspectFill"></image></view>
  </view>
</view>
<view class="img">
  <view class="title">治愈过程</view>
</view>
<view class="content">
  <rich-text class="rich-text" nodes="{{content}}"></rich-text>
</view>